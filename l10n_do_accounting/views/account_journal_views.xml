<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_journal_form" model="ir.ui.view">
        <field name="model">account.journal</field>
        <field name="name">account.journal.form</field>
        <field name="inherit_id" ref="l10n_latam_invoice_document.view_account_journal_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='type']" position="after">
                <field name="l10n_do_payment_form"
                    invisible="type not in ('cash','bank') or country_code != 'DO'"
                    required="type in ('cash','bank') and country_code != 'DO'"/>
            </xpath>
            <xpath expr="//page[@name='bank_account']/group" position="inside">
                <group name="document_types" string="Document Types"
                        invisible="not l10n_latam_use_documents or country_code != 'DO'">
                    <field name="l10n_do_document_type_ids" nolabel="1" colspan="2">
                        <list create="false" delete="false" editable="top"
                              default_order="l10n_latam_document_type_id">
                            <field name="journal_id" column_invisible="True"/>
                            <field name="l10n_latam_document_type_id" readonly="1"/>
                            <field name="sequence_start"/>
                            <field name="sequence_end"/>
                            <field name="current_sequence"/>
                            <field name="sequences_remaining" readonly="1"/>
                            <field name="sequence_state" widget="badge" 
                                   decoration-success="sequence_state == 'active'"
                                   decoration-warning="sequence_state == 'warning'"
                                   decoration-danger="sequence_state == 'exhausted'"
                                   decoration-muted="sequence_state == 'unconfigured'"/>
                            <field name="l10n_do_ncf_expiration_date"/>
                            <field name="warning_threshold" optional="hide"/>
                        </list>
                    </field>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
